{
  "$status": "KANONISK VERKTØYSEMANTIKK FOR MCP-SERVEREN. Serveren er bygget og testet på POST /api/public/mcp (Streamable HTTP, JSON-RPC 2.0). Denne filen dokumenterer semantikken; den er ikke i seg selv en klientkonfigurasjon — bruk common/connection.json og leverandørens README.",
  "$transport": {
    "type": "streamable-http",
    "endpoint": "/api/public/mcp",
    "methods": ["initialize", "notifications/initialized", "ping", "tools/list", "tools/call"],
    "protocol_versions": ["2025-11-25", "2025-06-18"],
    "stateless": true,
    "batch": false
  },
  "$auth": {
    "type": "oauth2.1",
    "resource": "{KARRIERENMIN_BASE_URL}/api/public/mcp",
    "metadata": "/.well-known/oauth-protected-resource/api/public/mcp",
    "scopes": ["karriere.status.read", "karriere.workflow.run"],
    "$note": "Ingen statisk nøkkel. Brukeren godkjenner scopene selv i Karrierenmin."
  },
  "tools": [
    {
      "name": "karrierenmin_status",
      "scope": "karriere.status.read",
      "description": "Henter status, driftsform, ubekreftede egenskaper og hvilke arbeidsflyter brukeren har slått på.",
      "inputSchema": { "type": "object", "properties": {}, "additionalProperties": false },
      "annotations": { "readOnlyHint": true, "idempotentHint": true, "openWorldHint": false },
      "on_error": "Ved integration_inactive: stopp all videre kontakt. Ved insufficient_scope: be brukeren godkjenne tilgangen på nytt."
    },
    {
      "name": "karrierenmin_run",
      "scope": "karriere.workflow.run",
      "description": "Ber om en arbeidsflyt. Oppretter aldri en kjøring og svarer alltid not_enabled eller not_available.",
      "inputSchema": {
        "type": "object",
        "required": ["workflow_kind"],
        "properties": {
          "workflow_kind": {
            "type": "string",
            "enum": ["job_import", "career_log", "linkedin_ready"]
          }
        },
        "additionalProperties": false
      },
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "openWorldHint": false },
      "on_success": "Finnes ikke. Verktøyet svarer alltid med en feilkode. Presenter aldri svaret som en utført jobb."
    }
  ],
  "$legacy_rest": {
    "$note": "Kompatibilitetslag for eksisterende integrasjonstokener. Ikke MCP.",
    "karrierenmin_claim": {
      "method": "POST",
      "path": "/api/public/ai-integrations/claim",
      "auth": "none",
      "$capabilities_note": "Capabilities sendes ikke. Serveren ignorerer klientpåstander og lagrer tomme, ubekreftede egenskaper. Aldri utledet fra abonnement."
    },
    "karrierenmin_status": {
      "method": "GET",
      "path": "/api/public/ai-integrations/v1/status",
      "auth": "bearer:integration_token"
    }
  }
}
